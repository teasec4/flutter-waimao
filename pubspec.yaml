name: paste_tool
description: "Paste Tool - быстрые фразы, словарь, калькулятор объёма"
publish_to: "none"

version: 2.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  go_router: ^17.2.2
  path_provider: ^2.1.5
  window_manager: ^0.4.3

  # Isar Community DB
  isar_community: ^3.3.2
  isar_community_flutter_libs: ^3.3.2
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Isar Generator
  isar_community_generator: ^3.3.2
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
